{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.5.2"
  },
  "scripts": {
    "release": "changeset publish && git push --follow-tags"
  }
}
